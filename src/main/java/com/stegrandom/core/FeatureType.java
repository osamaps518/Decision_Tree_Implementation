package com.stegrandom.core;

public enum FeatureType {
    CATEGORICAL,    // For discrete, categorical features like color, shape, etc.
    CONTINUOUS     // For continuous numerical features like height, weight, etc.
}